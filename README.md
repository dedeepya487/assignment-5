# assignment-5
The task-1 is to create a dictionary of students in which the keys were student names and the values are their mark.By taking the name of the student as the input from the user and traversing through the dictionary and if the name is in the dictionary then should print the marks of theat student ,else an message like student not found should be printed.
The task-2 is to create a list of numbers from 1 to 10 and then to extract first 5  umbers from the list and then reverse those numbers and should finally print the original list, extracted list and the reversed list
